⏺ GitHub Actions 部署流程说明

  根据 deploy-final.yml 文件，部署流程如下：

  触发条件

  - 推送到 main、develop 或 daily/* 分支时触发
  - 创建版本标签（v*）时触发

  部署主要流程

  1. 测试阶段 (test job)

  - 检出代码
  - 设置 Python 3.11 环境
  - 安装基础依赖
  - 运行单元测试（test_unit.py）

  2. 部署阶段 (deploy job)

  2.1 SSH连接设置
  - 使用 GitHub Secrets 中的 SSH 密钥连接到服务器
  - 服务器地址：SSH_HOST
  - 用户：SSH_USER（默认 root）

  2.2 停止旧服务
  pkill -f "uvicorn app:app"

  2.3 更新代码
  cd /root/mem0
  git fetch origin
  git checkout [分支名]
  git pull origin [分支名]

  2.4 配置管理
  - 备份现有 .env 文件
  - 使用 .env.server 覆盖配置
  - 从 GitHub Secrets 替换敏感信息：
    - DEEPSEEK_APIKEY
    - OPENAI_API_KEY
    - DATABASE_HOST/PORT/NAME/USER/PASSWORD
  - 自动生成 API_SECRET_KEY

  2.5 数据库初始化
  - 安装数据库依赖（pymysql）
  - 执行数据库初始化脚本：
    - scripts/init_database_sync.py 或
    - scripts/init_database.py

  2.6 依赖安装
  # 升级 SQLite（ChromaDB 需要）
  pip install pysqlite3-binary

  # 安装核心依赖
  pip install fastapi uvicorn pydantic python-dotenv pymysql sqlalchemy loguru

  # 安装完整依赖
  pip install -r requirements.txt

  # 安装 AI 依赖
  pip install sentence-transformers chromadb

  2.7 模型下载
  设置 HuggingFace 镜像并尝试三种方案下载中文嵌入模型：
  1. 使用项目脚本 download_chinese_model.py
  2. 直接下载 shibing624/text2vec-base-chinese
  3. 备用模型 paraphrase-multilingual-MiniLM-L12-v2

  2.8 启动服务
  nohup python3 -m uvicorn app:app --host 0.0.0.0 --port 9000 > logs/app.log
  2>&1 &

  2.9 健康检查
  - 循环检查 http://localhost:9000/health
  - 检查端口绑定（确保绑定到 0.0.0.0）
  - 检查防火墙规则
  - 如失败则自动重启

  关键配置（GitHub Secrets）

  必须在 GitHub 仓库设置中配置以下 Secrets：

  - SSH 连接：
    - SSH_HOST - 服务器地址
    - SSH_USER - SSH 用户（可选，默认 root）
    - SSH_PRIVATE_KEY - SSH 私钥
  - API 密钥：
    - DEEPSEEK_APIKEY - DeepSeek API 密钥
    - OPENAI_API_KEY - OpenAI API 密钥（可选）
  - 数据库配置：
    - DATABASE_HOST - 数据库地址
    - DATABASE_PORT - 数据库端口（默认 3306）
    - DATABASE_NAME - 数据库名称
    - DATABASE_USER - 数据库用户
    - DATABASE_PASSWORD - 数据库密码

  部署后访问

  - 服务地址：http://服务器IP:9000
  - API 文档：http://服务器IP:9000/docs

  这个自动化部署流程确保了代码推送后能自动更新到服务器并重启服务。